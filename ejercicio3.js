/*Realizar una tabla de multiplicación de un número (a) desde 0 hasta x, pero que no se muestre la multiplicación de a * 5.*/
function tablaMultiplicacion (a = 0 , x = 12) { /*creamos la funcion y le damos los paramatros */
    let  resultado = [];

    for (  )
}